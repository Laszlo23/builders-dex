[package]
name = "builder-passport"
version = "0.1.0"
description = "Builder Passport and Score on-chain program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "builder_passport"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]


[dependencies]
anchor-lang = "0.31.1"

